/* global _li */

var app = {
        wojciak: {
            items: {

            }
        }
    },
    init = {
        event: 'wojciak.init'
    },
    baseURL = '/';

_li.core.init(app, init, baseURL);